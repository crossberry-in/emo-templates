// scripts/reset-project.em — project reset script
// Run this to move the example code into an "app-example" directory
// and create a fresh blank app/ directory.
component ResetProject {
  render {
    <Column>
      <Text>Reset project script</Text>
      <Text>Run: emo run scripts/reset-project.em</Text>
    </Column>
  }
}
